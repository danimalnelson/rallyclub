{
  "name": "wine-club-saas",
  "version": "0.1.0",
  "private": true,
  "description": "B2B2C Wine Club SaaS Platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter web dev & pnpm --filter embed dev",
    "build": "pnpm --filter web build && pnpm --filter embed build",
    "db:generate": "pnpm --filter db generate",
    "db:push": "pnpm --filter db push",
    "db:migrate": "pnpm --filter db migrate",
    "db:seed": "pnpm --filter db seed",
    "db:studio": "pnpm --filter db studio",
    "lint": "pnpm --parallel lint",
    "test": "pnpm --parallel test",
    "test:e2e": "pnpm --filter web test:e2e"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "prettier": "^3.1.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}

