# ========================================
# DATABASE
# ========================================
# Required for Prisma and all database operations
# Get from: Neon dashboard → Connection String
DATABASE_URL=postgresql://username:password@host:5432/database?sslmode=require

# ========================================
# NEXTAUTH (Authentication)
# ========================================
# Required for NextAuth.js authentication
NEXTAUTH_URL=http://localhost:3000
# For production, use your actual domain:
# NEXTAUTH_URL=https://yourdomain.com

# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-here-generate-with-openssl

# ========================================
# STRIPE (Payments & Connect)
# ========================================
# Get from: Stripe Dashboard → Developers → API Keys
# Use test keys for development, live keys for production

# Secret key (server-side only)
STRIPE_SECRET_KEY=sk_test_...
# For production:
# STRIPE_SECRET_KEY=sk_live_...

# Publishable key (client-side safe)
STRIPE_PUBLISHABLE_KEY=pk_test_...
# For production:
# STRIPE_PUBLISHABLE_KEY=pk_live_...

# Webhook secret for signature verification
# Get from: Stripe Dashboard → Developers → Webhooks
STRIPE_WEBHOOK_SECRET=whsec_...

# ========================================
# RESEND (Email Service)
# ========================================
# Required for transactional emails
# Get from: Resend Dashboard → API Keys
RESEND_API_KEY=re_...

# Optional: Custom from address (defaults to onboarding@resend.dev)
EMAIL_FROM=noreply@yourdomain.com

# ========================================
# APPLICATION
# ========================================
# Public app URL for callbacks and emails
NEXT_PUBLIC_APP_URL=http://localhost:3000
# For production:
# NEXT_PUBLIC_APP_URL=https://yourdomain.com

# ========================================
# SETUP INSTRUCTIONS
# ========================================
# 1. Copy this file to .env.local:
#    cp .env.example .env.local
#
# 2. Fill in all required values above
#
# 3. Generate NEXTAUTH_SECRET:
#    openssl rand -base64 32
#
# 4. Get DATABASE_URL from Neon:
#    - Log into Neon dashboard
#    - Navigate to your database
#    - Copy the connection string
#    - Ensure ?sslmode=require is appended
#
# 5. Get Stripe keys from Stripe Dashboard:
#    - Navigate to Developers → API Keys
#    - Copy Secret Key (starts with sk_)
#    - Copy Publishable Key (starts with pk_)
#    - Create webhook endpoint for STRIPE_WEBHOOK_SECRET
#
# 6. Get Resend API key:
#    - Log into Resend dashboard
#    - Navigate to API Keys
#    - Create new key or copy existing
#
# 7. Verify setup:
#    pnpm prisma generate
#    pnpm prisma db push
#    pnpm build
#    bash scripts/run-full-tests.sh
